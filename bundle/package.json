{
  "name": "@working-from-lenny/bundle",
  "version": "0.9.0",
  "private": true,
  "description": "Working from Lenny — MCP App bundle source. Worked example: #44 PM Pitfalls Self-Audit.",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsx scripts/build-html.ts",
    "validate": "tsx scripts/validate.ts",
    "smoke": "tsx scripts/smoke-test.ts",
    "pack": "tsx scripts/pack.ts",
    "typecheck": "tsc --noEmit",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "catalog:",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "~5.9.2"
  }
}
