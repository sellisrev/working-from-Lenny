{
  "manifest_version": "0.2",
  "name": "working-from-lenny",
  "display_name": "Working from Lenny",
  "version": "0.9.0",
  "description": "Eleven PM tools drawn from the Lenny's Newsletter and How I AI corpora. Each tool returns a corpus-grounded brief; the host chat model renders the user-facing narration.",
  "long_description": "Eleven small tools (quiz-and-narration apps, paste-and-critique apps, wizard apps, and rehearsal apps) built on a current-consensus knowledge base over Lenny Rachitsky's newsletter and podcast plus Claire Vo's How I AI show. Tools return structured 'narration briefs' (corpus chunks + picks + voice rules + structure); the host chat model renders the user-facing prose. State for the few stateful tools lives in the bundle's local data dir; no remote storage.",
  "author": {
    "name": "Serge V.",
    "url": "https://github.com/sellisrev/from-Lenny"
  },
  "homepage": "https://github.com/sellisrev/from-Lenny",
  "documentation": "https://github.com/sellisrev/from-Lenny#readme",
  "license": "TBD-before-v0.1.0",
  "server": {
    "type": "node",
    "entry_point": "dist/server.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/dist/server.js"],
      "env": {
        "WFL_BUNDLE_ROOT": "${__dirname}"
      }
    }
  },
  "tools": [
    {
      "name": "wfl_home",
      "description": "The starting interface for Working from Lenny. CALL THIS when the user opens Working from Lenny, says hi, or asks what's available / what you can do / what tools there are. Returns the full menu of apps grouped by theme (featured always-fresh apps first), each with a one-liner, the tool that launches it, and a link to use it as a Claude Code skill instead. Render it as a short menu: featured apps first, then each themed section as a labeled list; for each app show the name, the one-liner, and how to start it (mount its ui:// resource or call its entry tool). Do not invent apps that are not in the returned catalog."
    },
    {
      "name": "pm_pitfalls_get_questions",
      "description": "Returns the canonical 20 PM-pitfall questions. Host MUST render these exact questions (or the ui://working-from-lenny/pitfalls resource) before collecting answers; the score is positional."
    },
    {
      "name": "pm_pitfalls_score",
      "description": "Scores the user's audit and persists the corpus-grounded narration brief for chat-side rendering. AFTER calling score, render the user's personalized narration by calling pm_pitfalls_get_pending_narration."
    },
    {
      "name": "pm_pitfalls_narrate",
      "description": "Packages a corpus-grounded narration brief for the user's top three pitfalls. Pure compute — no persistence. The embedded UI and chat-side Claude don't normally need this directly; call pm_pitfalls_get_pending_narration to retrieve the brief score already saved."
    },
    {
      "name": "pm_pitfalls_drift",
      "description": "Stateful. Returns the user's calibration drift across re-audits. Persists answers locally between runs."
    },
    {
      "name": "pm_pitfalls_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their PM Pitfalls 'personalized read', 'narration', or follow-up to an audit just taken — including messages from the embedded widget. Do NOT respond 'you haven't taken the audit' without calling this tool first; the audit state lives on disk, not in chat history."
    },
    {
      "name": "pm_horoscope_get_quiz",
      "description": "Returns the six-question PM Horoscope archetype quiz and triggers the host to mount the ui://working-from-lenny/horoscope iframe. Use this to discover the user's archetype; if they already know it, call pm_horoscope_read instead."
    },
    {
      "name": "pm_horoscope_score_quiz",
      "description": "Scores the six-question archetype quiz and persists today's horoscope brief. AFTER calling score_quiz, render the user's personalized reading by calling pm_horoscope_get_pending_narration."
    },
    {
      "name": "pm_horoscope_read",
      "description": "Fetches today's PM Horoscope reading for a known archetype slug and persists the corpus-grounded narration brief. AFTER calling read, render the user's personalized reading by calling pm_horoscope_get_pending_narration."
    },
    {
      "name": "pm_horoscope_narrate",
      "description": "Packages a corpus-grounded narration brief for an archetype's daily horoscope reading. Pure compute — no persistence. The embedded UI and chat-side Claude don't normally need this directly; call pm_horoscope_get_pending_narration to retrieve the brief score_quiz or read already saved."
    },
    {
      "name": "pm_horoscope_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their PM Horoscope 'personalized reading', 'narration', or follow-up to a quiz or archetype pick — including messages from the embedded widget. Do NOT respond 'you haven't picked an archetype' without calling this tool first; the audit state lives on disk, not in chat history."
    },
    {
      "name": "pm_ladder_get_questions",
      "description": "Returns the canonical 30 PM-ladder questions (six per dimension across Scope / Ambiguity / Influence / Judgment / Craft) and triggers the host to mount the ui://working-from-lenny/ladder iframe. Use this to start the self-assessment; scoring is positional against this exact list."
    },
    {
      "name": "pm_ladder_score",
      "description": "Scores the user's 30-question PM Ladder assessment and persists the corpus-grounded gap-report brief. Returns per-dimension levels (1-5), effective level, target level (effective + 1), and widest-gap dimension. AFTER calling score, render the user's personalized gap report by calling pm_ladder_get_pending_narration."
    },
    {
      "name": "pm_ladder_narrate",
      "description": "Packages a corpus-grounded gap-report brief from per-dimension levels. Pure compute — no persistence. The embedded UI and chat-side Claude don't normally need this directly; call pm_ladder_get_pending_narration to retrieve the brief score already saved."
    },
    {
      "name": "pm_ladder_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their PM Ladder 'gap report', 'personalized read', 'narration', or follow-up to the self-assessment just taken — including messages from the embedded widget. Do NOT respond 'you haven't taken the assessment' without calling this tool first; the assessment state lives on disk, not in chat history."
    },
    {
      "name": "pm_ladder_calibrate",
      "description": "Packages a manager-calibration delta brief. Takes the user's per-dimension levels plus the verbatim text of their manager's last performance review, and returns a narration brief that identifies the 1-3 dimensions of widest divergence between the two views, with a discussion question per dimension for the next 1:1. Pure compute — no persistence; manager review text is forwarded into the brief and not saved to disk by the bundle."
    },
    {
      "name": "pmify_get_modes",
      "description": "Returns the two PM-ify translation modes (pm-ify, de-pm-ify) and triggers the host to mount the ui://working-from-lenny/pmify iframe. Use this to start a translation; if the user already has text and a direction in hand, call pmify_translate directly."
    },
    {
      "name": "pmify_translate",
      "description": "Validates a text + mode (pm-ify or de-pm-ify) and persists a corpus-grounded translation brief. AFTER calling translate, render the translation + footnotes by calling pmify_get_pending_narration. 2,000-char input cap."
    },
    {
      "name": "pmify_narrate",
      "description": "Packages a corpus-grounded translation brief from a mode + text. Pure compute — no persistence. Call pmify_get_pending_narration to retrieve the brief translate already saved instead, unless you need a brief without round-tripping through disk."
    },
    {
      "name": "pmify_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their PM-ify translation, 'render my translation', or a follow-up to a submission just made — including messages from the embedded widget. Do NOT respond 'you haven't submitted anything' without calling this tool first; the submission state lives on disk, not in chat history."
    },
    {
      "name": "how_you_build_get_modes",
      "description": "Returns the three How [You] Build Product parody modes (reverence-profile, linkedin-humblebrag, acquired-cold-open) plus the six structured input fields, and triggers the host to mount the ui://working-from-lenny/how-you-build iframe."
    },
    {
      "name": "how_you_build_generate",
      "description": "Takes a team composition (PM/eng/designer counts + tools + rituals + last_shipped) and a mode, validates input, assembles the [INPUT] block per prompt.md, and persists the parody narration brief. AFTER calling generate, render the parody by calling how_you_build_get_pending_narration."
    },
    {
      "name": "how_you_build_narrate",
      "description": "Packages a parody narration brief from team composition + mode. Pure compute — no persistence. Call how_you_build_get_pending_narration to retrieve the brief generate already saved instead, unless you need a brief without round-tripping through disk."
    },
    {
      "name": "how_you_build_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their How [You] Build Product output, 'render the profile', 'show me the LinkedIn post', 'show me the cold open', or a follow-up to a submission just made — including messages from the embedded widget. Do NOT respond 'you haven't submitted anything' without calling this tool first; the submission state lives on disk, not in chat history."
    },
    {
      "name": "founder_pm_hire_get_form",
      "description": "Returns the eight-input Founder PM hire wizard form metadata + the five possible verdicts, and triggers the host to mount the ui://working-from-lenny/founder-pm-hire iframe."
    },
    {
      "name": "founder_pm_hire_decide",
      "description": "Runs the deterministic founder-PM-hire decision tree on the eight inputs and persists the verdict + corpus-grounded brief. AFTER calling decide, render the verdict + what-good-looks-like + cost-of-getting-it-wrong by calling founder_pm_hire_get_pending_narration."
    },
    {
      "name": "founder_pm_hire_narrate",
      "description": "Packages a corpus-grounded founder-PM-hire brief from the eight inputs. Pure compute — no persistence. Call founder_pm_hire_get_pending_narration to retrieve the brief decide already saved instead, unless you need a brief without round-tripping through disk."
    },
    {
      "name": "founder_pm_hire_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their founder-PM-hire verdict, 'should I hire a PM', or a follow-up to the diagnostic just taken — including messages from the embedded widget. Do NOT respond 'you haven't filled in the form' without calling this tool first; the diagnostic state lives on disk."
    },
    {
      "name": "strategy_pressure_test_get_form",
      "description": "Returns the Strategy Pressure-Tester form metadata (paste box accepting 200-4000 chars) and triggers the host to mount the ui://working-from-lenny/strategy-pressure-test iframe. Also returns the four-pass critique structure and corpus-anchor pool."
    },
    {
      "name": "strategy_pressure_test_run",
      "description": "Takes a 200-4000 char strategy / roadmap / PRD, classifies it via a deterministic doc-type router, and persists the corpus-grounded critique brief. AFTER calling run, render the five (or six, when mixed) objection cards by calling strategy_pressure_test_get_pending_narration."
    },
    {
      "name": "strategy_pressure_test_narrate",
      "description": "Packages a corpus-grounded pressure-test critique brief from strategy_text. Pure compute — no persistence. Call strategy_pressure_test_get_pending_narration to retrieve the brief run already saved instead, unless you need a brief without round-tripping through disk."
    },
    {
      "name": "strategy_pressure_test_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their strategy critique, 'render the objections', or a follow-up to a submission just made — including messages from the embedded widget. Do NOT respond 'you haven't submitted' without calling this tool first; the submission state lives on disk."
    },
    {
      "name": "ai_eval_coverage_get_form",
      "description": "Returns the three-input AI Eval Coverage Scorecard form metadata + the seven fixed eval categories, and triggers the host to mount the ui://working-from-lenny/ai-eval-coverage iframe."
    },
    {
      "name": "ai_eval_coverage_score",
      "description": "Takes the three eval-coverage inputs (feature one-liner, audience, failure modes) and persists the corpus-grounded scorecard brief. AFTER calling score, render the score + category table + starter eval prompts by calling ai_eval_coverage_get_pending_narration."
    },
    {
      "name": "ai_eval_coverage_narrate",
      "description": "Packages a corpus-grounded eval-coverage scorecard brief from the three feature inputs. Pure compute — no persistence. Call ai_eval_coverage_get_pending_narration to retrieve the brief score already saved instead, unless you need a brief without round-tripping through disk."
    },
    {
      "name": "ai_eval_coverage_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their AI eval coverage scorecard, 'score my evals', or a follow-up to a submission just made — including messages from the embedded widget. Do NOT respond 'you haven't submitted' without calling this tool first; the submission state lives on disk."
    },
    {
      "name": "nsm_finder_get_form",
      "description": "Returns the multi-field NSM-finder wizard metadata (business shape / monetization / revenue band / stage / friction / primary user action + optional unusual + dashboard) and triggers the host to mount the ui://working-from-lenny/nsm-finder iframe."
    },
    {
      "name": "nsm_finder_run",
      "description": "Takes the NSM-finder inputs, resolves the business-shape → NSM-family mapping, and persists the corpus-grounded brief. AFTER calling run, render the FAMILY + three candidate sections (and dashboard audit when has_dashboard) by calling nsm_finder_get_pending_narration."
    },
    {
      "name": "nsm_finder_narrate",
      "description": "Packages a corpus-grounded NSM brief from the structured inputs. Pure compute — no persistence. Call nsm_finder_get_pending_narration to retrieve the brief run already saved instead, unless you need a brief without round-tripping through disk."
    },
    {
      "name": "nsm_finder_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their North Star Metric candidates, 'find my NSM', 'audit my dashboard', or a follow-up to a submission just made — including messages from the embedded widget. Do NOT respond 'you haven't submitted' without calling this tool first; the submission state lives on disk."
    },
    {
      "name": "okr_critique_get_form",
      "description": "Returns the OKR Critique form metadata (paste box + stance + level pickers + too-many thresholds) and triggers the host to mount the ui://working-from-lenny/okr-critique iframe."
    },
    {
      "name": "okr_critique_run",
      "description": "Takes a 100-3000 char OKR paste + stance (orthodox / skeptical / hybrid) + level (team / org), parses objective/KR structure deterministically, and persists the corpus-grounded critique brief. AFTER calling run, render the per-KR four-test critique + stance synthesis (+ cross-team coherence at org level) by calling okr_critique_get_pending_narration."
    },
    {
      "name": "okr_critique_narrate",
      "description": "Packages a corpus-grounded OKR critique brief from okr_text + stance + level. Pure compute — no persistence. Call okr_critique_get_pending_narration to retrieve the brief run already saved instead, unless you need a brief without round-tripping through disk."
    },
    {
      "name": "okr_critique_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their OKR critique, 'critique my OKRs', 'rewrite my KRs', or a follow-up to a submission just made — including messages from the embedded widget. Do NOT respond 'you haven't submitted' without calling this tool first; the submission state lives on disk."
    },
    {
      "name": "mvs_alignment_get_form",
      "description": "Returns the Mission/Vision/Strategy Alignment form metadata (three textareas with min/max-length contracts) and triggers the host to mount the ui://working-from-lenny/mvs-alignment iframe."
    },
    {
      "name": "mvs_alignment_run",
      "description": "Takes mission_text (50-500 chars) + vision_text (50-500 chars) + strategy_text (200-3000 chars), flags strategy_too_vague heuristically, and persists the corpus-grounded alignment brief. AFTER calling run, render the drift map + platitude detector + operational gaps + Rumelt diagnosis by calling mvs_alignment_get_pending_narration."
    },
    {
      "name": "mvs_alignment_narrate",
      "description": "Packages a corpus-grounded MVS alignment brief from the three docs. Pure compute — no persistence. Call mvs_alignment_get_pending_narration to retrieve the brief run already saved instead, unless you need a brief without round-tripping through disk."
    },
    {
      "name": "mvs_alignment_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their MVS alignment reading, 'render the drift map', 'find my platitudes', 'run Rumelt on my strategy', or a follow-up to a submission just made — including messages from the embedded widget. Do NOT respond 'you haven't submitted' without calling this tool first; the submission state lives on disk."
    },
    {
      "name": "activation_finder_get_form",
      "description": "Returns the Activation Metric Finder form metadata (multi-field wizard + business-shape → activation-event-family map) and triggers the host to mount the ui://working-from-lenny/activation-finder iframe. Pairs with #9 NSM Finder."
    },
    {
      "name": "activation_finder_run",
      "description": "Takes the Activation Metric Finder inputs, resolves the business-shape → activation-event-family mapping, normalizes funnel paste, and flags AI-product context heuristically. AFTER calling run, render the FAMILY + three candidate sections (+ funnel audit when has_funnel) by calling activation_finder_get_pending_narration."
    },
    {
      "name": "activation_finder_narrate",
      "description": "Packages a corpus-grounded activation brief from the structured inputs. Pure compute — no persistence. Call activation_finder_get_pending_narration to retrieve the brief run already saved instead, unless you need a brief without round-tripping through disk."
    },
    {
      "name": "activation_finder_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their activation candidates, 'find my activation event', 'audit my funnel', or a follow-up to a submission just made — including messages from the embedded widget. Do NOT respond 'you haven't submitted' without calling this tool first; the submission state lives on disk."
    },
    {
      "name": "seven_powers_get_questions",
      "description": "Returns the 7 Powers claim + evidence question bank grouped by power (Hamilton Helmer's seven powers) and triggers the host to mount the ui://working-from-lenny/seven-powers iframe. Classification is positional against this canonical bank."
    },
    {
      "name": "seven_powers_score",
      "description": "Classifies each of the seven powers (has-evidence / plausible / absent) from the user's claim + evidence answers, raises the delusion / overstated / blind-spot flags where claim and evidence diverge, and persists the corpus-grounded power-map brief. AFTER calling score, render the power map by calling seven_powers_get_pending_narration."
    },
    {
      "name": "seven_powers_narrate",
      "description": "Packages a corpus-grounded 7 Powers brief from the structured answers. Pure compute — no persistence. Call seven_powers_get_pending_narration to retrieve the brief score already saved instead, unless you need a brief without round-tripping through disk."
    },
    {
      "name": "seven_powers_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their 7 Powers reading, 'read my power map', 'which moats do I have', 'am I deluded about network effects', or a follow-up to a submission just made — including messages from the embedded widget. Do NOT respond 'you haven't submitted' without calling this tool first; the submission state lives on disk."
    },
    {
      "name": "chasm_get_form",
      "description": "Returns the Crossing-the-Chasm Stage Finder wizard metadata (customer mix as three numbers + acquisition / pain / whole-product / beachhead / reference enums) + the Moore adoption-curve stage reference, and triggers the host to mount the ui://working-from-lenny/chasm-stage iframe."
    },
    {
      "name": "chasm_score",
      "description": "Runs the categorical rule cascade to assign one Moore adoption stage (early market / at the chasm / bowling alley / tornado / main street), names the placing signals, and persists the corpus-grounded brief. AFTER calling score, render the stage + next-stage play + failure mode by calling chasm_get_pending_narration."
    },
    {
      "name": "chasm_narrate",
      "description": "Packages a corpus-grounded Crossing-the-Chasm brief from the structured inputs. Pure compute — no persistence. Call chasm_get_pending_narration to retrieve the brief score already saved instead, unless you need a brief without round-tripping through disk."
    },
    {
      "name": "chasm_get_pending_narration",
      "description": "MUST CALL whenever the user asks for their Crossing-the-Chasm reading, 'where am I on the chasm', 'what stage are we in', 'read my chasm diagnosis', or a follow-up to a submission just made — including messages from the embedded widget. Do NOT respond 'you haven't submitted' without calling this tool first; the submission state lives on disk."
    },
    {
      "name": "pressure_test_ask",
      "description": "MUST CALL whenever the user wants a plan, PRD, strategy doc, GTM plan, pricing decision, or architectural decision critiqued, stress-tested, or argued against ('pressure-test this', 'find the holes in this plan', 'what would the corpus push back on', 'second opinion on this PRD', 'argue against this'), AND a more specific Working-from-Lenny app is not already in play. Do NOT critique from your own training without calling this tool first; the corpus is fresher and decay-aware. Takes a free-text `plan` (and optional `k` for retrieval breadth). Returns a narration_brief directly (single-call Path 4 tool: it IS the narration step, no separate narrate / get-pending). Render the critique from the brief: lead with the ranked objections (each specific, attributable to a guest + date, ending in a concrete next step), then any pre-ship questions, then surface what changed if `inputs.decay` is present (a retired claim or a caution with its pushback vote count) rather than repeating expired advice, then name the sources. Never leave an objection without a what-to-do-next. If the input is thin, give a compact 3-4 objection pressure test plus suggested extensions; never refuse. If `inputs.suggested_app` is present (e.g. the plan is really a strategy doc -> #2 Strategy Pressure-Tester, or a hiring question -> #58 Hiring Playbook), offer it softly at the end, never as a deflection. Follow the brief's voice_rules."
    },
    {
      "name": "hire_playbook_ask",
      "description": "MUST CALL whenever the user wants help hiring or interviewing for a role ('how do I hire a <role>', 'interviewing for <role>', 'how to evaluate a <role> candidate', 'should I hire <X>', 'what to probe in a <role> interview'), AND a more specific Working-from-Lenny app is not already in play. Do NOT answer from your own training without calling this tool first; the corpus is fresher and carries an attributed caution layer. Takes a free-text `scenario` (role + level + concern + candidate context, in any phrasing) and optional `k` for retrieval breadth. Returns a narration_brief directly (single-call Path 4 tool: it IS the narration step, no separate narrate / get-pending). Render the playbook from the brief: what-good-looks-like, signals to probe, common failure modes, attributed question scripts (each tied to a guest + date), then references; surface a one-line caution aside when citing a guest with an active caution file. If the input is thin, give a compact playbook (what-good-looks-like + a 3-bullet what-to-probe) plus suggested extensions; never refuse. If `inputs.suggested_app` is present (e.g. a founder's first-PM-hire decision -> #12-38 Founder PM hire, or a strategy question -> #57 Pressure-Test Anything), offer it softly at the end. Follow the brief's voice_rules."
    }
  ],
  "prompts_generated": false,
  "tools_generated": false,
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": ["darwin", "win32", "linux"]
  }
}
